{% extends 'base.html.twig' %}

{% block title %}New client{% endblock %}

{% block body %}
  <div class="mb-3 d-flex justify-content-between align-items-center">
    <h1 class="h4 m-0">🆕 New client</h1>
    <a href="{{ path('app_client_index') }}" class="btn btn-sm btn-outline-light rounded-1">
      ← Back to list
    </a>
  </div>

  <div class="card bg-dark border-0 p-4" style="max-width: 640px;">
    {{ include('client/_form.html.twig', { button_label: 'Create client' }) }}
  </div>
{% endblock %}
