{{ form_start(form) }}
<div class="vstack gap-3">
  <div>
    <label class="form-label mb-1">📝 Title</label>
    {{ form_widget(form.title, {
      attr: form.title.vars.attr|merge({
        'class':'form-control',
        'placeholder':'E.g. Face close-up, bold text “NEW VIDEO”'
      })
    }) }}
    {{ form_errors(form.title) }}
  </div>

  <div>
    <label class="form-label mb-1">📄 Brief</label>
    {{ form_widget(form.brief, {
      attr: form.brief.vars.attr|merge({
        'class':'form-control',
        'rows':8,
        'placeholder':'Tell us what you need: style, mood, colors, assets…'
      })
    }) }}
    {{ form_errors(form.brief) }}
  </div>
  <div class="row g-3">
    <div class="col-12 col-md-6">
      <label class="form-label mb-1">⏰ Due at</label>
      {% set dueAttr = form.dueAt.vars.attr|merge({'class':'form-control'}) %}
      {% if form.children['client'] is not defined %}
        {% set dueAttr = dueAttr|merge({'min': min_due_at|default(null)}) %}
      {% endif %}
      {{ form_widget(form.dueAt, { attr: dueAttr }) }}
      {{ form_errors(form.dueAt) }}
      {% if due_min_help|default(false) %}
        <div class="form-text text-secondary">
          Due date must be in the future.
        </div>
      {% endif %}
    </div>
    <div class="col-12 col-md-6">
      <label class="form-label mb-1">💶 Price</label>
      <div class="input-group">
        <input type="text" class="form-control js-price-euros"
               name="pretty_price"
               value=""
               placeholder="e.g., 15,00" autocomplete="off">
        <span class="input-group-text">€</span>
      </div>
      {{ form_widget(form.price) }}
      {{ form_errors(form.price) }}

      <div class="d-flex flex-wrap gap-2 mt-2">
        {% set presets = [500,1000,1500,2000,2500,3000] %}
        {% if lastPriceCents is defined and lastPriceCents %}
          <button type="button"
                  class="btn btn-sm btn-outline-primary rounded-1 js-preset"
                  data-cents="{{ lastPriceCents }}">
            Last: {{ (lastPriceCents/100)|number_format(2, ',', ' ') }} €
          </button>
        {% endif %}
        {% for c in presets %}
          <button type="button"
                  class="btn btn-sm btn-outline-secondary rounded-1 js-preset"
                  data-cents="{{ c }}">
            {{ (c/100)|number_format(2, ',', ' ') }} €
          </button>
        {% endfor %}
      </div>
      <div class="form-text text-secondary">Minimum 5,00 €.</div>
    </div>
  </div>
  {% set inputId = form.attachments.vars.id %}
  <div>
    <label class="form-label mb-1">📎 Attach files (PDF or images)</label>
    {{ form_widget(form.attachments, {
      attr: form.attachments.vars.attr|merge({
        'class':'form-control',
        'id': inputId,
        'accept':'image/png,image/jpeg,image/webp,application/pdf',
        'multiple':'multiple'
      })
    }) }}
    {{ form_errors(form.attachments) }}
    {% if existingAttachments is defined and existingAttachments %}
      <div class="row g-1 mt-2" id="{{ inputId }}__existing">
        {% for att in existingAttachments %}
          <div class="col-4 col-md-3 col-lg-2">
            <div class="thumb">
              {% if att.isImage %}
                <img
                  src="{{ att.url|imagine_filter('order_attachment_preview') }}"
                  alt="{{ att.name }}"
                >
              {% else %}
                <div class="pdf-icon d-flex align-items-center justify-content-center">
                  <span class="badge text-bg-light text-dark">PDF</span>
                </div>
              {% endif %}
              <div class="small text-truncate mt-1 text-secondary">
                {{ att.name }}
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    {% endif %}
    <div id="{{ inputId }}__preview" class="row g-1 mt-2"></div>

    <div class="form-text text-secondary">PNG/JPG/WebP/PDF, up to 25 MB each.</div>
  </div>
  {% set hasAdminFields =
    (form.children['client'] is defined) or
    (form.children['status'] is defined) or
    (form.children['paid'] is defined)
  %}
  {% if hasAdminFields %}
    <div class="row g-3 pt-2">
      {% if form.children['client'] is defined %}
        <div class="col-12 col-md-4">
          <label class="form-label mb-1">👤 Client</label>
          {{ form_widget(form.client, {
            attr: form.client.vars.attr|merge({'class':'form-select'})
          }) }}
          {{ form_errors(form.client) }}
        </div>
      {% endif %}

      {% if form.children['status'] is defined %}
        <div class="col-12 col-md-4">
          <label class="form-label mb-1">🏷️ Status</label>
          {{ form_widget(form.status, {
            attr: form.status.vars.attr|merge({'class':'form-select'})
          }) }}
          {{ form_errors(form.status) }}
        </div>
      {% endif %}

      {% if form.children['paid'] is defined %}
        <div class="col-12 col-md-4">
          <label class="form-label mb-1 d-block">💰 Paid</label>
          <div class="form-check form-switch">
            {{ form_widget(form.paid, {
              attr: form.paid.vars.attr|merge({
                'class':'form-check-input',
                'id': form.paid.vars.id ~ '_switch'
              })
            }) }}
            <label class="form-check-label small"
                   for="{{ form.paid.vars.id }}_switch">
              Mark this order as paid
            </label>
          </div>
          {{ form_errors(form.paid) }}
        </div>
      {% endif %}
    </div>
  {% endif %}

  <div class="d-flex gap-2 pt-2">
    <button class="btn btn-primary rounded-1">
      {{ button_label|default('Save') }}
    </button>
    {% if back_path is defined and back_path %}
      <a class="btn btn-outline-light rounded-1" href="{{ back_path }}">Cancel</a>
    {% endif %}
  </div>
</div>

{{ form_end(form) }}

<style>
  .form-label { display: block; }

  #{{ inputId }}__existing .thumb,
  #{{ inputId }}__preview .thumb {
    position: relative;
    border: 1px solid rgba(255,255,255,.15);
    border-radius: .4rem;
    padding: .3rem;
    background: rgba(255,255,255,.03);
    max-width: 110px;
    margin-inline: auto;
  }

  #{{ inputId }}__existing img,
  #{{ inputId }}__existing .pdf-icon,
  #{{ inputId }}__preview img,
  #{{ inputId }}__preview .pdf-icon {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: .35rem;
    background: rgba(255,255,255,.05);
  }

  #{{ inputId }}__preview .btn-remove {
    position: absolute;
    top: .15rem;
    right: .15rem;
    width: 1.6rem;
    height: 1.6rem;
    padding: 0;
    border-radius: 999px;
    border: none;
    font-size: .85rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(220,53,69,.95);
    color: #fff;
    box-shadow: 0 0 0 1px rgba(0,0,0,.5);
    cursor: pointer;
  }

  #{{ inputId }}__preview .btn-remove:hover {
    background: #dc3545;
    transform: scale(1.05);
  }

  #{{ inputId }}__existing .thumb .small,
  #{{ inputId }}__preview .thumb .small {
    font-size: .75rem;
  }
</style>

<script>
(function(){
  const centsInput  = document.querySelector('input[name$="[price]"]');
  const prettyInput = document.querySelector('.js-price-euros');
  const presets     = document.querySelectorAll('.js-preset');

  function fmt(c){
    return (c/100).toLocaleString(undefined,{
      minimumFractionDigits:2,
      maximumFractionDigits:2
    });
  }

  function parseToCents(str){
    if(!str) return NaN;
    const n = String(str).replace(/[€\s]/g,'').replace(',','.');
    const f = parseFloat(n);
    if(isNaN(f)) return NaN;
    return Math.round(f*100);
  }

  function syncFromPretty(){
    const c = parseToCents(prettyInput.value);
    if(!isNaN(c)){
      const v = Math.max(500,c);
      centsInput.value = String(v);
      prettyInput.value = fmt(v);
    }
  }

  function setCents(c){
    const v = Math.max(500, c|0);
    centsInput.value = String(v);
    prettyInput.value = fmt(v);
  }

  if (centsInput && prettyInput) {
    if (centsInput.value) {
      const existing = parseInt(centsInput.value, 10);
      if (!isNaN(existing) && existing > 0) {
        prettyInput.value = fmt(existing);
      } else {
        setCents(500);
      }
    } else {
      setCents(500);
    }
  }

  prettyInput?.addEventListener('change', syncFromPretty);
  prettyInput?.addEventListener('blur', syncFromPretty);

  presets.forEach(b =>
    b.addEventListener('click', () =>
      setCents(parseInt(b.dataset.cents || '0', 10))
    )
  );
})();
</script>

<script>
(function initAttachments(){
  const input   = document.getElementById('{{ inputId }}');
  const preview = document.getElementById('{{ inputId }}__preview');
  if (!input || !preview) return;

  let dt = window.DataTransfer ? new DataTransfer() : null;

  function rebuildFrom(dtLike){
    preview.innerHTML = '';
    const files = dtLike ? dtLike.files : input.files;

    Array.from(files).forEach((file, idx) => {
      const col  = document.createElement('div');
      col.className = 'col-4 col-md-3 col-lg-2';

      const card = document.createElement('div');
      card.className = 'thumb';

      const close = document.createElement('button');
      close.type  = 'button';
      close.className = 'btn-remove';
      close.textContent = '×';

      close.addEventListener('click', () => {
        if (dt) {
          const tmp = new DataTransfer();
          Array.from(dt.files).forEach((f,i) => {
            if (i !== idx) tmp.items.add(f);
          });
          dt = tmp;
          input.files = dt.files;
        } else {
          const arr = Array.from(input.files).filter((_,i) => i !== idx);
          const tmp = new DataTransfer();
          arr.forEach(f => tmp.items.add(f));
          input.files = tmp.files;
        }
        rebuildFrom(dt);
      });

      let media;
      if (file.type && file.type.startsWith('image/')) {
        media = document.createElement('img');
        media.alt = file.name;
        const r = new FileReader();
        r.onload = e => media.src = e.target.result;
        r.readAsDataURL(file);
      } else {
        media = document.createElement('div');
        media.className = 'pdf-icon d-flex align-items-center justify-content-center';
        media.innerHTML = '<span class="badge text-bg-light text-dark">PDF</span>';
      }

      const cap = document.createElement('div');
      cap.className = 'small text-truncate mt-1 text-secondary';
      cap.textContent = file.name;

      card.appendChild(close);
      card.appendChild(media);
      card.appendChild(cap);
      col.appendChild(card);
      preview.appendChild(col);
    });
  }

  input.addEventListener('change', () => {
    if (dt) {
      Array.from(input.files).forEach(f => dt.items.add(f));
      input.files = dt.files;
    }
    rebuildFrom(dt);
  });

  document.addEventListener('turbo:load', () => rebuildFrom(dt));
})();
</script>
