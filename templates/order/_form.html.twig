{# templates/order/_form.html.twig #}
{{ form_start(form) }}

{{ form_row(form.title,  { label: '📝 Title' }) }}
{{ form_row(form.brief,  { label: '📄 Brief' }) }}
{{ form_row(form.dueAt,  { label: '⏰ Due at' }) }}

{# Champs présents seulement pour l’admin : on les affiche si et seulement s’ils existent #}
{% if form.children['price'] is defined %}
  {{ form_row(form.children['price'], { label: '💶 Price (cents)' }) }}
{% endif %}

{% if form.children['client'] is defined %}
  {{ form_row(form.children['client'], { label: '👤 Client' }) }}
{% endif %}

{% if form.children['status'] is defined %}
  {{ form_row(form.children['status'], { label: '🏷️ Status' }) }}
{% endif %}

{% if form.children['createdAt'] is defined %}
  {{ form_row(form.children['createdAt'], { label: 'Created at' }) }}
{% endif %}

{% if form.children['updatedAt'] is defined %}
  {{ form_row(form.children['updatedAt'], { label: 'Updated at' }) }}
{% endif %}

<button class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
